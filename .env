# .env is better suited for public variables, ie, variables that should not commited
# For secret variables is better to use DevServerControl tool with set_env_variable: ["KEY", "SECRET"]

# https://www.builder.io/c/docs/using-your-api-key
VITE_PUBLIC_BUILDER_KEY=__BUILDER_PUBLIC_KEY__
PING_MESSAGE="ping pong"

# Email Configuration (use DevServerControl to set these)
# Option 1: Gmail SMTP (requires Gmail app password)
# GMAIL_USER=your-email@gmail.com
# GMAIL_APP_PASSWORD=your-app-specific-password

# Option 2: Webhook Service (e.g., Zapier, Make, or custom webhook)
# WEBHOOK_URL=https://your-webhook-service.com/mail-endpoint

# Email notifications will be sent to: akira@hkjewel.co
